# 科研论文 Agent 系统 — 对话留存

本文件记录每次对话的核心内容，用于开发对接。新对话在前。

---

## 2026-07-27 对话 #3：LLM 厂商接入 + 5 阶段深化（借鉴开源项目）

### 用户核心诉求

1. 调研主流厂商 token plan，倾向 MiniMax 与小米 MiMo 基础套餐，评估是否够用
2. 继续推进项目，每个部分必须做好，借鉴 skill 等开源工作

### Token Plan 调研结论（2026-07-27 最新）

**MiniMax Token Plan（主力推荐）**
| 套餐 | 价格 | M3 月度额度 | Agent 并发 | 上下文 |
|---|---|---|---|---|
| Plus | ¥49/月 | 6亿+ token | 3-4 | 1M |
| Max | ¥119/月 | 18亿+ token | 4-5 | 1M |
| Ultra | ¥469/月 | 71亿+ token | 6-7 | 1M |

**小米 MiMo Token Plan（6月9日降价后）**
| 套餐 | 价格 | Credits/月 | 任务量基准 |
|---|---|---|---|
| Lite | ¥39/月 | 41亿（4.1B） | ~200轮中等任务 |
| Standard | ¥99/月 | 110亿（11B） | ~1600轮 |
| Pro | ¥329/月 | 380亿（38B） | ~5600轮 |
| Max | ¥659/月 | 820亿（82B） | ~12800轮 |

夜间 0:00-8:00 享 0.8x 消耗，无 5h 限额。mimo-v2.5-pro 消耗：缓存命中2.5C/未命中300C/输出600C per token。

**DeepSeek V4（按量）**：输入(缓存命中)¥0.025/M、未命中¥3/M、输出¥6/M，与 MiMo 按量同价，编程/推理补强。

**够用性评估**（按一轮完整科研流程约 3-4M tokens 估算）：
- MiniMax Plus ¥49/月：6亿token可跑约150轮，**完全够用，推荐主力**
- MiMo Lite ¥39/月：深度使用偏紧，适合夜间长上下文备用
- MiMo Standard ¥99/月：约1700万输出token，**稳妥够用**

**推荐组合**：MiniMax Plus 主力 + MiMo Lite 夜间备用 + DeepSeek 按量编程补强 + local 敏感任务

### 本轮完成的工作

#### 1. LLM 适配层：接入 MiniMax / MiMo / DeepSeek

- **`core/config.py`**：LLMConfig 新增 minimax/mimo/deepseek 的 api_key_env、base_url_env、base_url_default 字段
- **`config/tasks.yaml`**：新增 research_subquery_decompose / paper_relevance_filter / research_cross_validate / design_atom_decompose / experiment_code_generate / experiment_code_review / writing_outline / writing_section_draft 等 task_type，按厂商优势路由（minimax 主力 / mimo 长上下文 / deepseek 编程 / local 敏感）
- **`core/llm/registry.py`**：from_config 方法新增 minimax/mimo/deepseek 三个 provider 自动注册逻辑，复用 OpenAIProvider（三家均兼容 OpenAI 协议）

#### 2. research 阶段深化（借鉴 PaperQA + GPT-Researcher）

节点从 5 个扩展为 8 个：
```
TopicRefineAgent
→ SubqueryDecomposeAgent（GPT-Researcher：子问题分解，6 个正交侧面）
→ StageCheckpoint
→ TopicConfirmHuman（用户可修订子问题，支持 subq: 语法）
→ PaperFetchAgent（按子问题并行检索，带 source_subquery 标记）
→ PaperRelevanceFilterAgent（PaperQA filter：相关性打分+筛选，阈值 0.5）
→ PaperIngestAgent（chunk 摘要向量入库，借鉴 PaperQA 摘要入库）
→ CrossValidateAgent（GPT-Researcher：多源交叉验证，输出可信度报告）
```

新增域键：RESEARCH_SUBQUERIES / RESEARCH_FILTERED_PAPER_METAS / RESEARCH_CROSS_VALIDATION_REPORT

设计要点：
- 子问题分解覆盖动机/方法/数据/评估/局限/扩展 6 个正交侧面，每子问题带检索意图（arxiv/s2/web）
- PaperFetch 每候选带 source_subquery 标记，便于交叉验证溯源
- PaperRelevanceFilter 保留 rejected 列表含 reason，便于人工追溯
- CrossValidate 输出 conflicts/consensus/gaps/overall_confidence，低于 0.6 阈值建议回滚

#### 3. design 阶段深化（借鉴 AI-Researcher 原子概念分解）

节点从 5 个扩展为 6 个：
```
AtomDecomposeAgent（AI-Researcher：原子概念分解，建立公式↔代码映射）
→ MethodFormalizeAgent（整合原子概念为方法文档）
→ StageCheckpoint
→ MethodReviewHuman
→ ClaimEvidenceLinkAgent（从方法派生 Claim，关联证据）
→ MethodArtifactAgent（生成方法 Artifact）
```

新增域键：DESIGN_ATOM_CONCEPTS / DESIGN_FORMULA_CODE_MAP

AtomDecomposeAgent 输出原子概念结构 `{concept_name, description, formula_latex, code_stub, dependencies}`，确保论文公式与代码一一对应。

#### 4. experiment 阶段深化（借鉴 AI-Researcher 导师-学生迭代）

节点从 5 个扩展为 7 个：
```
ExperimentConfigAgent
→ CodeGenerateAgent（AI-Researcher Code Agent：DeepSeek 生成代码）
→ CodeReviewAgent（AI-Researcher Advisor Agent：MiniMax 审查，MAX_REVIEW_ROUNDS=3）
→ StageCheckpoint
→ ExperimentRunTool（ToolNode 执行实验）
→ AnomalyCheckAgent（loss spike/NaN/不收敛检测）
→ ClaimVerifyAgent（用实验结果回填 Claim 证据）
```

新增域键：EXPERIMENT_CODE / EXPERIMENT_REVIEW_NOTES

CodeGenerateAgent 读 DESIGN_FORMULA_CODE_MAP 校验代码忠实实现公式；CodeReviewAgent 多轮迭代由 GraphRunner 外部循环驱动（DAG 不支持环）。

#### 5. writing 阶段深化（借鉴 AI-Researcher 层级式生成）

节点从 6 个扩展为 7 个（DraftAgent 拆为 OutlineAgent + SectionDraftAgent）：
```
ProvenanceCheckTool（溯源链硬校验，未验证 Claim/未完成 Experiment 拒绝）
→ StyleLearnAgent
→ OutlineAgent（AI-Researcher：大纲，每章关联 claim_ids + target_word_count）
→ StageCheckpoint
→ SectionDraftAgent（AI-Researcher：按章撰写，MiMo 1M 上下文装载全部素材）
→ ReviewAgent（三维度审稿：结构/引证/表达）
→ ReviseHuman
```

新增域键：WRITING_OUTLINE / WRITING_SECTIONS

ProvenanceCheckTool 调用 ProvenanceValidator.validate_for_writing，失败返回 NodeStatus.FAILED 附结构化失败原因。

### 验证导向达标

- 每个节点显式声明 input_schema / output_schema / output_keys 三件套
- _execute 内 LLM 调用以完整注释范式给出（含 task_type、system/prompt、structured_output 解析），实际用占位数据返回，不产生 API 费用
- 占位 ID 均由 KnowledgeStore.new_id() 生成
- design/experiment/writing 三个 subagent 均通过 AST 语法检查与冒烟测试

### 借鉴的开源项目设计要点汇总

| 开源项目 | 借鉴的设计 | 落地节点 |
|---|---|---|
| GPT-Researcher | 子问题分解（Planner） | SubqueryDecomposeAgent |
| GPT-Researcher | 并行检索 + source 标记 | PaperFetchAgent |
| GPT-Researcher | 交叉验证（多源冲突检测） | CrossValidateAgent |
| PaperQA | 工具化 RAG filter | PaperRelevanceFilterAgent |
| PaperQA | chunk 摘要入库 | PaperIngestAgent |
| AI-Researcher | 原子概念分解 + 公式↔代码映射 | AtomDecomposeAgent |
| AI-Researcher | Code Agent + Advisor Agent 迭代 | CodeGenerateAgent + CodeReviewAgent |
| AI-Researcher | 层级式论文生成（大纲→填充→校对） | OutlineAgent + SectionDraftAgent + ReviewAgent |

### 待后续工作

- ideation 阶段（思路探讨）尚未深化，可借鉴 LangGraph 人工介入节点设计
- 安装依赖后跑 pytest 验证全部用例
- 各 stage 节点的 _execute 从占位转为真实 LLM 调用，端到端验证最小科研流程
- 引入 ProjectSession 与 stages graph 的端到端串联

---

## 2026-07-27 对话 #2：横切基础架构全量落地 + 5 阶段占位 + 单元测试

### 用户核心诉求

1. 开始落地全部代码，**验证导向**：每个子 agent 输入输出设计要清晰
2. 仅根据代码实现情况迭代优化，**先不运行测试**以避免不必要的 API 费用损失

### 本轮完成的工作

按"先建横切基础，再填阶段实现"的策略，一次性落地全部骨架代码：

#### 1. 项目配置层
- `requirements.txt`：pydantic / sqlalchemy / chromadb / openai / anthropic / pyyaml 等
- `pyproject.toml`：setuptools 构建 + pytest 配置 + ruff lint
- `.gitignore`：忽略 projects/ / .env / *.db / vectors/ / mlruns/ 等
- `config/tasks.yaml`：12 个 task_type 的 provider/model/temperature 路由配置（覆盖 5 阶段 + 嵌入）

#### 2. core/ 横切基础层（5 子模块）

**core/state（状态机层）**
- `lifecycle.py`：`LifecycleStage` 5 阶段枚举（RESEARCH/IDEATION/DESIGN/EXPERIMENT/WRITING）+ `StageStatus` 5 子状态 + 合法流转表
- `transition.py`：`TransitionValidator` 校验阶段内/跨阶段/前进/回滚流转，`TransitionResult` 流转记录
- `snapshot.py`：`Snapshot` 快照 + `SnapshotStore` JSON 持久化（含索引）+ `make_snapshot` 工厂
- `session.py`：`ProjectSession` 项目会话，封装 create/load/start/pending_review/blocked/complete/advance/rollback_to/restore_from_snapshot 全流转

**core/knowledge（知识库层）**
- `schema.py`：5 种实体（Paper/Idea/Claim/Experiment/Artifact）+ PaperChunk + Relation + 7 种关系类型，全部 pydantic BaseModel
  - **Claim 硬约束**：用 `model_validator(mode="after")` 校验非 DRAFT 状态必须有 evidence_refs，每条 evidence_ref 必须含 type(paper/experiment) 与 id
- `store.py`：`KnowledgeStore` SQLite+JSON 持久化，线程安全，7 张表（papers/paper_chunks/ideas/claims/experiments/artifacts/relations），含索引与外键
- `vector_store.py`：`VectorStore` 抽象 + `ChromaVectorStore` 本地持久化实现，支持按 paper_id 过滤检索
- `retriever.py`：`Retriever` 高层检索，支持文献语义检索、Artifact 溯源链查询、Claim 证据查询、Experiment 反向查询

**core/llm（LLM 适配层）**
- `base.py`：`LLMProvider` 抽象基类 + `LLMRequest/LLMResponse/EmbeddingRequest/EmbeddingResponse/StructuredOutputRequest` IO 模型
- `task_router.py`：`TaskRouter` 从 YAML 加载，`TaskConfig` 单任务配置，`EmbeddingConfig` 嵌入配置
- `registry.py`：`LLMRegistry` 统一调用入口，complete/structured_output/embed 三方法，按 task_type 路由，凭据从环境变量读取
- `providers/openai_provider.py`：OpenAI 兼容（含本地部署），结构化输出用 response_format=json_object + JSON Schema 注入 + Pydantic 校验
- `providers/anthropic_provider.py`：Claude，结构化输出用 tool use 强制

**core/artifacts（产出物层）**
- `version.py`：`ArtifactContentStore` 文件存储 + `ArtifactManager` 版本管理（create_artifact/new_version，自动按内容大小决定存 DB 或文件，按 artifact_type 推断后缀）
- `provenance.py`：`ProvenanceChain` DAG 溯源链 + `ProvenanceValidator` 构建链路与校验
  - `validate_for_writing`：进入写作阶段的硬校验，断链/未验证 Claim/未完成 Experiment 全部拒绝

**core/orchestration（编排层）**
- `context.py`：`ContextKey[T]` 强类型键 + `ExecutionContext` 上下文（get/set/has/delete/snapshot/restore）
- `node.py`：`Node` 抽象基类 + 4 具体节点（AgentNode/HumanNode/ToolNode/CheckpointNode）+ `NodeInput/NodeOutput/NodeResult/NodeStatus/HumanRequest/HumanResponse`
  - AgentNode 子类化范式：声明 input_schema/output_schema/output_keys，实现 _build_input/_execute，框架自动 IO 编排
  - HumanNode：执行返回 PENDING_HUMAN + HumanRequest，由 GraphRunner 暂停，resume_after_human 继续
- `graph.py`：`Graph` DAG（add_node/add_edge/validate 含环检测与可达性）+ `GraphRunner` 执行引擎（拓扑序执行、人工节点阻塞、失败回退到 CheckpointNode、用户回滚）
- `registry.py`：`NodeRegistry` 节点类型注册表

#### 3. stages/ 5 个生命周期占位

每个 stage 含 `__init__.py` / `io_schema.py` / `agents.py` / `graph.py`，由 subagent 实现：

- `stages/common.py`：集中定义 4 个系统依赖注入键（LLM_REGISTRY/KNOWLEDGE_STORE/ARTIFACT_MANAGER/PROVENANCE_VALIDATOR）+ 全部跨阶段域键 + StageCheckpoint 基类
- **research**：TopicRefineAgent → StageCheckpoint → TopicConfirmHuman → PaperFetchAgent → PaperIngestAgent
- **ideation**：BrainstormAgent → IdeaDiscussHuman → StageCheckpoint → IdeaValidateAgent → ClaimDraftAgent
- **design**：MethodFormalizeAgent → StageCheckpoint → MethodReviewHuman → ClaimEvidenceLinkAgent → MethodArtifactAgent
- **experiment**：ExperimentConfigAgent → ExperimentRunTool → StageCheckpoint → AnomalyCheckAgent → ClaimVerifyAgent
- **writing**：ProvenanceCheckTool → StyleLearnAgent → StageCheckpoint → DraftAgent → ReviewAgent → ReviseHuman

**占位实现原则**：每个节点 _execute 从 context 取依赖（展示注入范式），LLM 调用以完整注释给出（含 task_type、prompt 构造、structured_output 解析），实际用 KnowledgeStore.new_id() 生成占位 ID 返回，不产生 API 费用。

#### 4. tests/ 18 个测试文件（263 用例，未运行）

由 subagent 实现，覆盖：
- 状态机：枚举/流转/快照/会话全流转（含回滚）
- 知识库：schema 校验（含 Claim 硬约束）/ CRUD / 向量库 / 检索器
- LLM：TaskRouter 加载 / Registry 路由（mock provider）
- 产出物：版本管理 / 溯源链校验（断链/未验证/未完成全拒）
- 编排：Context / Node / Graph 校验（环/不可达）/ GraphRunner 人工节点阻塞
- stages：5 个 build_*_graph 拓扑校验

`conftest.py` 提供 8 个 fixtures：tmp_project_paths / knowledge_store / artifact_manager / mock_llm_provider / mock_tasks_yaml / mock_task_router / mock_llm_registry / fake_embedding_fn

### 代码审查修复的 3 个 bug

1. **`lifecycle.py` prev_stage 引用未定义变量**：改为 `ordered = self.ordered()` 局部变量
2. **`ProjectSession.create()` 初始快照自流转被拒**：NOT_STARTED→NOT_STARTED 不在 LEGAL_STATUS_TRANSITIONS，改为直接构造 TransitionResult 跳过校验（初始化非真正流转）
3. **`Claim` 的 field_validator 失效**：evidence_refs 在 status 之前定义，info.data 不含 status 导致硬约束校验被跳过。改用 `model_validator(mode="after")` 在所有字段赋值后校验

清理的 2 处垃圾代码：
- `GraphRunner._handle_failure` 中 `cp_key = type(self._ctx).__name__` 占位行
- `GraphRunner.resume_after_human` 中 `isinstance(node, type(...))` 冗余检查

### 项目结构（约 65 文件）

```
scientificResearchAgentSys/
├── config/tasks.yaml               # 12 task_type 路由配置
├── core/                           # 横切基础层
│   ├── config.py                   # ProjectPaths / LLMConfig / GlobalConfig
│   ├── state/                      # lifecycle/transition/snapshot/session
│   ├── knowledge/                  # schema/store/vector_store/retriever
│   ├── llm/                        # base/task_router/registry/providers/
│   ├── artifacts/                  # version/provenance
│   └── orchestration/              # context/node/graph/registry
├── stages/                         # 5 生命周期占位
│   ├── common.py                   # 依赖注入键 + StageCheckpoint
│   ├── research/  ideation/  design/  experiment/  writing/
├── tests/                          # 18 文件 263 用例（未运行）
├── pyproject.toml  requirements.txt  .gitignore
└── 对话留存.md
```

### 验证闭环设计要点

1. **每个节点显式 IO**：input_schema/output_schema/output_keys 三件套，框架自动写回 context
2. **Claim 硬约束**：非 DRAFT 必须有 evidence_refs，writing 阶段前 ProvenanceValidator.validate_for_writing 强校验
3. **状态机快照链**：每次流转落盘快照，parent_snapshot_id 构成回滚路径
4. **人工节点阻塞**：GraphRunner 暴露 is_pending_human，UI 层消费 HumanRequest 后 resume_after_human
5. **失败回退**：失败时回退到最近 CheckpointNode 的 context 快照

### 待后续工作

- 安装依赖后运行 pytest 验证 263 用例（预期可能还有少量接口不一致需修）
- 各 stage 节点的 _execute 从占位转为真实 LLM 调用（按 task_type 路由）
- 引入 ProjectSession 与 stages graph 的端到端串联（编排框架注入依赖到 ExecutionContext）
- 实验运行阶段的真实实验执行（DVC/MLflow 集成）
- 论文写作阶段的 LaTeX 模板与格式适配

### 下一步建议

1. 先 `pip install -r requirements.txt` 后跑一次 pytest，根据失败用例做接口微调
2. 跑通后，从 research 阶段开始，把占位 _execute 替换为真实 LLM 调用，端到端验证一个最小科研流程
3. 验证通过后，再逐步替换 ideation/design/experiment/writing 阶段

---

## 2026-07-27 对话 #1：项目愿景确立 + 横切基础架构初稿

### 用户核心诉求

构建一个能产出"推导可信、贡献实在、实验正确、方法有效"论文的科研 Agent 系统。要避免产出"学术外观合格但内容空洞"的论文。

提出 5 个生命周期拆解：
1. **调研**：确定主题、获取基础知识、查阅相关论文
2. **思路探讨**：交互式与人探讨、思路记录与验证、完成论文核心设计约束
3. **方案制定**：方法公式可视化、引证观点联系透明化
4. **实验运行**：版本管理、中间结果可视化、异常监测
5. **论文写作**：格式适配、写作风格学习、修改指导

5 阶段可相互回滚，逐步产出论文。

### 关键约定

- **每次对话最后**留存核心到本文件，完成后返回"约定完成"
- 用户偏好：详细规划、审慎考虑每个细节、单线程实验（RTX 3060）、强理论保证 + 清晰实验验证

### 本轮决策（用户选择）

| 维度 | 决策 |
|------|------|
| MVP 起点 | **横切基础先建**（状态 + 知识库 + 编排） |
| 技术栈 | **暂不定，按模块需求选型** |
| LLM 接入 | **可配置，按阶段任务灵活切换** |
| 项目形态 | **暂不定，先按架构接口设计** |

### 横切基础架构 v0.1 设计稿

#### 目录骨架

```
scientificResearchAgentSys/
├── core/                          # 横切基础层（先建）
│   ├── state/                     # 生命周期状态机 + 快照 + 回滚
│   ├── knowledge/                 # 统一知识库（实体/关系/检索）
│   ├── orchestration/             # Agent编排（DAG/人工节点/上下文）
│   ├── llm/                       # LLM适配（任务路由/可配置）
│   ├── artifacts/                 # 产出物版本管理 + 溯源链
│   └── config.py
├── stages/                        # 5生命周期实现（后续逐个填充）
│   ├── research/  ideation/  design/  experiment/  writing/
├── projects/                      # 具体科研项目数据（git忽略）
└── tests/
```

#### 核心抽象：5 种实体 + 关系（贯穿全生命周期）

| 实体 | 产生阶段 | 关键字段 | 关系 |
|-----|---------|---------|------|
| `Paper` | 调研 | metadata, chunks, embedding | 被 Claim 引证 |
| `Idea` | 思路探讨 | text, constraints, status | 派生 Claim；引用 Paper |
| `Claim` | 方案制定 | statement, evidence_refs, status | 引证 Paper/Experiment；被 Artifact 引用 |
| `Experiment` | 实验运行 | config, results, artifacts | 验证 Claim |
| `Artifact` | 任阶段 | type, version, content | 引用 Claim/Experiment |

**硬约束**：每个 `Claim` 必须有 `evidence_refs`（指向 Paper chunk 或 Experiment run），否则不允许进入论文写作阶段。这是"推导可信"的核心保证。

#### 状态机

- 5 个 `LifecycleStage`：RESEARCH / IDEATION / DESIGN / EXPERIMENT / WRITING
- 每阶段内部状态：`not_started / in_progress / pending_review / done / blocked`
- 跨阶段流转：可前进、可回滚到任意前序阶段快照
- 快照内容：阶段状态 + 上下文 + 产出物版本号 + 触发回滚原因

#### 编排层

- 节点类型：`AgentNode` / `HumanNode`（阻塞等待）/ `ToolNode` / `CheckpointNode`
- 每个生命周期是一个子图，子图可嵌套
- `HumanNode` 用于：思路探讨（核心决策）、方案确认、实验异常介入

#### LLM 适配层

- 配置驱动：`tasks.yaml` 声明每个 task_type 用哪个 provider/model
- 统一接口：`complete / chat / embed / structured_output`
- 敏感任务（如未公开思路）可路由到本地模型，通用任务路由到商业 API

#### 跨阶段数据流

```
[调研] → Paper 入库
   ↓
[思路探讨] → Idea（引 Paper）+ Claim（待验证）
   ↓
[方案制定] → Claim 锁定 + 方法 Artifact
   ↓
[实验运行] → Experiment（验 Claim）+ 结果 Artifact
   ↓
[论文写作] → 终稿 Artifact（引全部上述实体）
   ↑____回滚可任一层____|
```

### 待用户确认的 3 个设计权衡

1. **知识库存储**：倾向 SQLite+JSON 起步，溯源链用"关系表+JSON"形式，后续复杂度上升再迁图库（Neo4j）
2. **状态机实现**：倾向自研轻量状态机 + 借鉴 LangGraph 思路，保持接口抽象，后续可换实现
3. **向量检索**：倾向 ChromaDB 起步（零部署、单机够用）

三个倾向都遵循"按模块需求选型、最小依赖起步"原则。

### 下一步

- 用户确认上述 3 个权衡后，开始落地 `core/` 骨架代码
- 若用户对某个权衡有不同看法，先对齐再动手

### 开源项目候选（待后续按模块选型时评估）

- 调研：arxiv API, Semantic Scholar API, PaperQA, GPTResearcher
- 思路探讨：LangGraph（人工介入节点）
- 方案制定：LaTeX+TikZ, Mermaid, 引用图谱工具
- 实验运行：DVC, MLflow/Wandb, Hydra
- 论文写作：LaTeX 模板, 审稿模拟 Agent

---
