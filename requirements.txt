# 科研论文 Agent 系统依赖
# Python >= 3.10

# Web 服务（HF Spaces / 本地）
fastapi>=0.110.0
uvicorn>=0.23.2
python-multipart>=0.0.9

# 核心数据建模
pydantic>=2.5.0

# 状态持久化
sqlalchemy>=2.0.0

# 向量检索（可选：无 chromadb 时自动降级）
chromadb>=0.4.22

# LLM 适配
openai>=1.12.0
anthropic>=0.21.0

# 配置与工具
pyyaml>=6.0
python-dotenv>=1.0.0

# 文献检索（research 阶段真实调用）
arxiv>=2.1.0
requests>=2.31.0

# 实验运行阶段（后续启用）
# mlflow>=2.10.0
# dvc>=3.40.0
# hydra-core>=1.3.2

# 论文写作阶段（后续启用）
# pylatex>=1.4.2